[package]
name = "buddy"
version = "1.1.0"
description = "Buddy chat desktop"
authors = ["Buddy"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "1", features = [] }

[dependencies]
tauri = { version = "1" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
